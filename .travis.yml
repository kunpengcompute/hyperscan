arch:
  - arm64

addons:
  hosts:
    - node1

script:
  - apt-get install libboost-all-dev
  - apt-get install ragel
  - apt-get install libpcre3-dev
  
  - cd ../
  - rm -rf build
  - mkdir build
  - cd build
  - cmake .. 
  - make -j16